---
import { Icon } from "astro-icon/components";
---

<div id="welcome">
  <section>
    <div class="welcome-img">
      <img
        src="https://smartcms-media.s3.ap-south-1.amazonaws.com/e6b05202-c737-4ce8-95a1-6132364f716e/conversions/why-coding-essential-future-webp.webp"
        alt="Avatar de Programa"
      />
    </div>
    <div class="welcome-text">
      <h1
        >Hola, Soy George Contreras <Icon
          name="mdi:code"
          height={40}
          width={40}
        /></h1
      >
      <span>Programador Web FullStack Junior </span>
      <p>
        Soy George Contreras, desarrollador frontend junior apasionado por
        aprender y mejorar constantemente.
      </p>
      <div class="welcome-a">
        <a href="https://www.linkedin.com/in/georgecontreras241" target="_blank"
          ><Icon name="mdi:linkedin" height={50} width={50} />
        </a>
        <a href="https://github.com/GeorgeContreras241" target="_blank"
          ><Icon name="mdi:github" height={50} width={50} />
        </a>
      </div>
    </div>
  </section>
</div>

<style>
  #welcome {
    display: grid;
    place-items: center;
    height: 90dvh;
    position: relative;
    z-index: 100;
  }
  section {
    width: clamp(100px, 95%, 1200px);
    margin: auto;
    display: flex;
    flex-flow: row;
    gap: 20px;
  }
  .welcome-img {
    width: clamp(100px, 90%, 550px);
    height: clamp(100px, 90%, 550px);
    aspect-ratio: 16/9;
    margin: auto;
    mask-image: radial-gradient(
      circle,
      rgba(0, 0, 0, 1) 30%,
      rgba(0, 0, 0, 0.5) 45%,
      rgba(0, 0, 0, 0.2) 90%,
      rgba(0, 0, 0, 0) 70%
    );

    -webkit-mask-image: radial-gradient(
      circle,
      rgba(0, 0, 0, 1) 20%,
      rgba(0, 0, 0, 0.5) 35%,
      rgba(0, 0, 0, 0.2) 50%,
      rgba(0, 0, 0, 0) 70%
    );
    box-shadow: 0 60px 60px -60px rgba(0, 30, 255, 0.5);
  }
  .welcome-img > img {
    max-width: 100%;
    height: auto;
  }
  .welcome-text {
    display: flex;
    flex-flow: column;
    justify-content: center;
    gap: 20px;
    min-width: 400px;
    width: 100%;
  }
  .welcome-text > h1 {
    font-size: clamp(1.5rem, 5vw, 3rem);
    color: var(--color-white);
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .welcome-text > span {
    font-size: clamp(1.2rem, 2vw, 1.7rem);
    color: var(--color-primary);
    filter: drop-shadow(0 0 10px var(--color-primary));
  }
  .welcome-text > p {
    font-size: 1.1rem;
    color: var(--color-text);
    font-family: "Roboto", sans-serif;
  }
  .welcome-a {
    display: flex;
    flex-flow: row;
    gap: 20px;
    justify-content: flex-end;
    margin-right: 2.5rem;
  }
  .welcome-a > a {
    font-size: clamp(0.8rem, 1.9vw, 1rem);
    color: var(--color-white);
    text-decoration: none;
    transition:
      transform 0.5s ease,
      color 0.3s ease;
  }
  .welcome-a > a:first-child:hover {
    color: var(--color-primary);
    transform: scale(1.1);
  }
  .welcome-a > a:nth-child(2):hover {
    color: var(--color-text);
    transform: scale(1.1);
  }
  .welcome-a > a:nth-child(3):hover {
    color: var(--color-accent);
    transform: scale(1.1);
  }

  @media screen and (max-width: 800px) {
    section {
      flex-flow: column;
    }
    .welcome-text {
      min-width: 100px;
    }
    .welcome-img {
      width: clamp(100px, 70%, 300px);
      height: clamp(100px, 80%, 300px);
    }
  }
</style>
