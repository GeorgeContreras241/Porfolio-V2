---
import Background from "../components/Background.astro";
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta
      name="Porfolio V2"
      content="Este es mi nuevo porfolio, he creado con Astro."
    />
    <title>Mi Porfolio</title>
  </head>
  <body>
    <Background />

    <main class="main">
      <slot />
    </main>
  </body>
</html>

<style is:global>
  * {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
    scrollbar-color: var(--color-primary) var(--color-bg);
    scrollbar-width: thin;
  }
  html,
  body {
    width: 100%;
    background-color: var(--color-bg);
    color: var(--color-text);
    scroll-behavior: smooth;
  }
  :root {
    /* 🎨 Colores principales */
    --color-bg: #1e1e1e; /* Fondo principal */
    --color-bg-secondary: #252525; /* Fondo secundario (cards, secciones) */
    --color-text: #e0e0e0; /* Texto principal */
    --color-text-secondary: #b0b0b0; /* Texto secundario, subtítulos */
    --color-white: #ffffff; /* Blanco humo, para títulos y mayor contraste */

    /* 🔵 Colores de interacción */
    --color-primary: #007bff; /* Azul brillante (botones, enlaces) */
    --color-primary-hover: #0056b3; /* Azul oscuro (hover en botones) */
    --color-accent: #ffa500; /* Naranja (llamados a la acción) */

    /* 💡 Sombras y bordes */
    --shadow-light: 0px 4px 10px rgba(0, 0, 0, 0.3);
    --shadow-hover: 0px 6px 15px rgba(0, 0, 0, 0.5);
    --border-light: 1px solid #3a3a3a; /* Bordes sutiles */

    /*Typography*/
    --font-title: "Poppins", sans-serif;
    --font-subtitle: "Raleway", sans-serif;
    --font-body: "Nunito", sans-serif;
    --font-button: "Roboto", sans-serif;
  }
  body {
    font-family: var(--font-body);
    background: var(--color-bg);
    color: var(--color-text);
  }

  h1,
  h2,
  h3 {
    font-family: var(--font-title);
    color: var(--color-white);
  }

  h2 {
    font-family: var(--font-subtitle);
  }

  p {
    font-family: var(--font-body);
  }

  button {
    font-family: var(--font-button);
    background: var(--color-primary);
    color: var(--color-white);
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    transition: background 0.3s ease-in-out;
  }

  button:hover {
    background: var(--color-primary-hover);
  }
</style>
